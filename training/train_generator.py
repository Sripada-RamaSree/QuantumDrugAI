"""Placeholder entry for future deep generative molecule models.

This project includes a functional property-guided mutation optimizer in optimize_molecules.py.
For an SCI extension, this file can be expanded into a VAE/RL molecular generator.
"""
from training.optimize_molecules import optimize

if __name__ == "__main__":
    optimize()
